package com.liberty.discovoadorntk.core.utils

object AppConstants {

    // GRUPO
    const val GROUP_ID = "groupId"
    const val GROUP_NAME = "groupName"

    // MENSAGEM
    const val MESSAGE_ID = "messageId"
    const val SENDER_ID = "senderId"
    const val SENDER_NAME = "senderName"
    const val IS_MUTED = "isMuted"

    // ALARM ACTIVITY
    const val MESSAGE_BODY = "messageBody"
    const val TIMESTAMP = "timestamp"

    // ALARM SERVICE
    const val ACTION_START_ALARM = "AS_ACTION_START_ALARM"
    const val ACTION_STOP_ALARM  = "AS_ACTION_STOP_ALARM"

    // ID único do canal de notificação usado para agrupar notificações de alarme
    const val ALARM_CHANNEL_ID = "alarm_dv_channel"
}